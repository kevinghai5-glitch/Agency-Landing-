/**
 * SINGLE SOURCE OF TRUTH for brand identity.
 *
 * The business is being renamed. Every brand string on the page comes from
 * here — there are ZERO hardcoded brand names in components. To rebrand,
 * change these constants and nothing else.
 */
export const BRAND_NAME = "LaunchGrid";
export const FOUNDER_NAME = "Kevin Ghai";
export const CONTACT_EMAIL = "hello@launchgrid.co";

/**
 * Path to the founder photo asset in /public. Placeholder for now — drop a real
 * photo in /public and point this at it (e.g. "/founder.jpg").
 */
export const FOUNDER_PHOTO = "/founder.svg";

/** Where the minimal footer's privacy link points. */
export const PRIVACY_URL = "/privacy";
